syntax = "proto3";
package chat;
option go_package = "github.com/ari1021/grpc-handson/chat;chat";

message Message {
  string body = 1;
}

service ChatService {
  rpc SayHello(Message) returns (Message) {}
}

